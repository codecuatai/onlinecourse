# Sửa trong tệp: onlinecourse/.htaccess

<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # 🚨 THÊM DÒNG NÀY (Đổi tên thư mục nếu cần)
    RewriteBase /trainning/onlinecourse/   
    
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
